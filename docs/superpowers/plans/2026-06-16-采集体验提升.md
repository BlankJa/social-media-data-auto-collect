# 采集体验提升 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让非技术同事能用——`cli.py status` 一眼看出谁的 cookie 该重扫，`cli.py account` 免编 YAML 地增删查与批量导入账号。

**Architecture:** 纯 CLI 增强，零新依赖。新增 `src/collector/accounts.py` 放账号增删查/导入的纯逻辑（与 CLI 表现层分离、可单测）；在 `status.py` 加 `cookie_advice` 纯函数 + 新面板；`cli.py` 接线。采集端不改——新账号无水位线，下次 collect 自动全量首采。

**Tech Stack:** Python 3.11、typer、rich、pyyaml、openpyxl（均为现有依赖）；pytest + tmp_path。

参考 spec：`docs/superpowers/specs/2026-06-16-采集体验提升-design.md`

---

## 文件结构

- 新建 `src/collector/accounts.py` —— 账号增删查导入的纯逻辑（无 CLI、无网络）
- 新建 `tests/test_accounts.py` —— accounts.py 单测
- 新建 `tests/fixtures/import_sample.csv` 与 `tests/fixtures/import_sample.xlsx` —— 导入样例
- 修改 `src/collector/status.py` —— 加 `COOKIE_AGE_WARN_DAYS`、`cookie_advice`、`render_status_panel`，删旧 `render_status_table`
- 修改 `tests/test_status.py` —— 补 `cookie_advice` 用例
- 修改 `cli.py` —— `status` 改用新面板；新增 `account` 子命令组

---

## Task 1: accounts.py —— 读取与安全写回 + 加账号去重

**Files:**
- Create: `src/collector/accounts.py`
- Test: `tests/test_accounts.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_accounts.py
from pathlib import Path

from collector.accounts import add_account, load_accounts_raw


def test_add_then_load(tmp_path: Path):
    assert add_account(tmp_path, "kuaishou", "3xabc", "复旦大学") is True
    rows = load_accounts_raw(tmp_path, "kuaishou")
    assert rows == [{"account_id": "3xabc", "account_name": "复旦大学"}]


def test_add_dedup(tmp_path: Path):
    add_account(tmp_path, "kuaishou", "3xabc", "复旦大学")
    # 同 id 再加返回 False，不重复
    assert add_account(tmp_path, "kuaishou", "3xabc", "复旦(改名)") is False
    assert len(load_accounts_raw(tmp_path, "kuaishou")) == 1


def test_written_file_has_header(tmp_path: Path):
    add_account(tmp_path, "kuaishou", "3xabc", "复旦大学")
    text = (tmp_path / "kuaishou.yaml").read_text("utf-8")
    assert text.startswith("# account_id 是快手个人主页")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_accounts.py -q`
Expected: FAIL（`ModuleNotFoundError: collector.accounts`）

- [ ] **Step 3: 写实现**

```python
# src/collector/accounts.py
from __future__ import annotations

from pathlib import Path

import yaml

PLATFORMS = ("bilibili", "weibo", "douyin", "kuaishou")

_HEADERS = {
    "bilibili": "# 每行一个账号；account_id 是 B 站 mid",
    "weibo": "# account_id 是数字 uid，从 weibo.com/u/<uid> 地址栏复制",
    "douyin": "# account_id 是抖音 sec_uid，从 douyin.com/user/<sec_uid> 地址栏复制",
    "kuaishou": "# account_id 是快手个人主页 URL 末段（kuaishou.com/profile/<id>）",
}


def _config_path(config_root: Path, platform: str) -> Path:
    return config_root / f"{platform}.yaml"


def load_accounts_raw(config_root: Path, platform: str) -> list[dict]:
    """读 configs/<platform>.yaml，返回 [{account_id, account_name}, ...]；缺文件返回 []。"""
    p = _config_path(config_root, platform)
    if not p.exists():
        return []
    data = yaml.safe_load(p.read_text("utf-8")) or []
    # account_id 一律规整成 str（防 yaml 把纯数字读成 int）
    out = []
    for r in data:
        out.append(
            {"account_id": str(r["account_id"]), "account_name": str(r.get("account_name", ""))}
        )
    return out


def write_accounts(config_root: Path, platform: str, accounts: list[dict]) -> None:
    """结构化写回，并在文件头重写该平台的说明注释。"""
    config_root.mkdir(parents=True, exist_ok=True)
    body = yaml.safe_dump(accounts, allow_unicode=True, sort_keys=False)
    _config_path(config_root, platform).write_text(
        _HEADERS[platform] + "\n" + body, encoding="utf-8"
    )


def add_account(config_root: Path, platform: str, account_id: str, account_name: str) -> bool:
    """加一个账号。已存在（同 account_id）返回 False 且不改文件，否则写入返回 True。"""
    accounts = load_accounts_raw(config_root, platform)
    if any(a["account_id"] == str(account_id) for a in accounts):
        return False
    accounts.append({"account_id": str(account_id), "account_name": str(account_name)})
    write_accounts(config_root, platform, accounts)
    return True
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_accounts.py -q`
Expected: PASS（3 passed）

- [ ] **Step 5: Commit**

```bash
git add src/collector/accounts.py tests/test_accounts.py
git commit -m "feat(accounts): load_accounts_raw + add_account 去重写回"
```

---

## Task 2: accounts.py —— 删账号

**Files:**
- Modify: `src/collector/accounts.py`
- Test: `tests/test_accounts.py`

- [ ] **Step 1: 写失败测试（追加进 tests/test_accounts.py）**

```python
from collector.accounts import remove_account


def test_remove_existing(tmp_path: Path):
    add_account(tmp_path, "kuaishou", "3xabc", "复旦大学")
    add_account(tmp_path, "kuaishou", "3xdef", "另一个")
    assert remove_account(tmp_path, "kuaishou", "3xabc") is True
    rows = load_accounts_raw(tmp_path, "kuaishou")
    assert [r["account_id"] for r in rows] == ["3xdef"]


def test_remove_missing(tmp_path: Path):
    add_account(tmp_path, "kuaishou", "3xabc", "复旦大学")
    assert remove_account(tmp_path, "kuaishou", "3xzzz") is False
    assert len(load_accounts_raw(tmp_path, "kuaishou")) == 1
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_accounts.py -k remove -q`
Expected: FAIL（`ImportError: cannot import name 'remove_account'`）

- [ ] **Step 3: 写实现（追加进 src/collector/accounts.py）**

```python
def remove_account(config_root: Path, platform: str, account_id: str) -> bool:
    """删一个账号。删到了写回返回 True；不存在返回 False。"""
    accounts = load_accounts_raw(config_root, platform)
    kept = [a for a in accounts if a["account_id"] != str(account_id)]
    if len(kept) == len(accounts):
        return False
    write_accounts(config_root, platform, kept)
    return True
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_accounts.py -q`
Expected: PASS（5 passed）

- [ ] **Step 5: Commit**

```bash
git add src/collector/accounts.py tests/test_accounts.py
git commit -m "feat(accounts): remove_account"
```

---

## Task 3: accounts.py —— account_id 格式校验（仅警告）

**Files:**
- Modify: `src/collector/accounts.py`
- Test: `tests/test_accounts.py`

- [ ] **Step 1: 写失败测试（追加）**

```python
from collector.accounts import validate_account_id


def test_validate_ok():
    assert validate_account_id("bilibili", "17616721") is None
    assert validate_account_id("kuaishou", "3xiz35fp79yc3cu") is None
    assert validate_account_id("douyin", "MS4wLjABAAAA") is None
    assert validate_account_id("weibo", "1729332983") is None


def test_validate_warns():
    # B站该是纯数字
    assert validate_account_id("bilibili", "abc") is not None
    # 快手该 3x 开头
    assert validate_account_id("kuaishou", "zzz") is not None
    # 抖音该 MS4w 开头
    assert validate_account_id("douyin", "123") is not None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_accounts.py -k validate -q`
Expected: FAIL（`ImportError: cannot import name 'validate_account_id'`）

- [ ] **Step 3: 写实现（追加）**

```python
def validate_account_id(platform: str, account_id: str) -> str | None:
    """按平台对 account_id 做轻量格式提示。不符返回警告串，正常返回 None。仅警告不拦截。"""
    aid = str(account_id)
    if platform in ("bilibili", "weibo"):
        if not aid.isdigit():
            return f"{platform} 的 account_id 一般是纯数字，当前 '{aid}' 可能填错"
    elif platform == "douyin":
        if not aid.startswith("MS4w"):
            return f"抖音 sec_uid 一般以 MS4w 开头，当前 '{aid}' 可能填错"
    elif platform == "kuaishou":
        if not aid.startswith("3x"):
            return f"快手主页 id 一般以 3x 开头，当前 '{aid}' 可能填错"
    return None
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_accounts.py -k validate -q`
Expected: PASS（2 passed）

- [ ] **Step 5: Commit**

```bash
git add src/collector/accounts.py tests/test_accounts.py
git commit -m "feat(accounts): validate_account_id 格式提示"
```

---

## Task 4: accounts.py —— 批量导入解析（.csv + .xlsx）

**Files:**
- Modify: `src/collector/accounts.py`
- Create: `tests/fixtures/import_sample.csv`、`tests/fixtures/import_sample.xlsx`
- Test: `tests/test_accounts.py`

- [ ] **Step 1: 造样例 fixture（csv）**

新建 `tests/fixtures/import_sample.csv`，内容（含一行无效平台名 `tieba`）：

```csv
platform,account_id,account_name
kuaishou,3xaaa,账号A
bilibili,123456,账号B
tieba,xxx,无效平台
```

- [ ] **Step 2: 造样例 fixture（xlsx）—— 用脚本生成**

Run:
```bash
uv run python - <<'PY'
from openpyxl import Workbook
wb = Workbook(); ws = wb.active
ws.append(["platform", "account_id", "account_name"])
ws.append(["douyin", "MS4wLjABAAAAxyz", "账号C"])
ws.append(["kuaishou", "3xbbb", "账号D"])
ws.append(["", "", ""])  # 空行应被忽略
wb.save("tests/fixtures/import_sample.xlsx")
print("written")
PY
```
Expected: 打印 `written`

- [ ] **Step 3: 写失败测试（追加）**

```python
FIXTURES = Path(__file__).parent / "fixtures"


def test_parse_import_csv():
    valid, errors = parse_import_file(FIXTURES / "import_sample.csv")
    assert [(v["platform"], v["account_id"]) for v in valid] == [
        ("kuaishou", "3xaaa"), ("bilibili", "123456")
    ]
    assert len(errors) == 1  # tieba 那行
    assert errors[0][0] == 4  # 第4行（含表头第1行）


def test_parse_import_xlsx():
    valid, errors = parse_import_file(FIXTURES / "import_sample.xlsx")
    assert [v["account_id"] for v in valid] == ["MS4wLjABAAAAxyz", "3xbbb"]
    assert errors == []  # 空行被忽略，不算错误
```

加到文件顶部的 import：
```python
from collector.accounts import parse_import_file
```

- [ ] **Step 4: 跑测试确认失败**

Run: `uv run pytest tests/test_accounts.py -k parse_import -q`
Expected: FAIL（`ImportError: cannot import name 'parse_import_file'`）

- [ ] **Step 5: 写实现（追加）**

```python
import csv

from openpyxl import load_workbook


def _read_csv(path: Path) -> list[dict]:
    with path.open(encoding="utf-8-sig", newline="") as f:
        return list(csv.DictReader(f))


def _read_xlsx(path: Path) -> list[dict]:
    wb = load_workbook(path, read_only=True)
    ws = wb.active
    rows = list(ws.iter_rows(values_only=True))
    if not rows:
        return []
    header = [str(h).strip() if h is not None else "" for h in rows[0]]
    out = []
    for r in rows[1:]:
        out.append({header[i]: (r[i] if i < len(r) else None) for i in range(len(header))})
    return out


def parse_import_file(path: Path) -> tuple[list[dict], list[tuple[int, str]]]:
    """解析导入文件，返回 (合法行, 错误行)。
    合法行：[{platform, account_id, account_name}]；account_name 为空时用 account_id 兜底。
    错误行：[(行号, 原因)]，行号含表头（数据从第 2 行起）。全空行忽略，不算错误。
    """
    path = Path(path)
    suffix = path.suffix.lower()
    if suffix == ".csv":
        raw = _read_csv(path)
    elif suffix == ".xlsx":
        raw = _read_xlsx(path)
    else:
        raise ValueError(f"不支持的导入格式：{suffix}（仅 .csv / .xlsx）")

    valid: list[dict] = []
    errors: list[tuple[int, str]] = []
    for i, r in enumerate(raw, start=2):  # 第 1 行是表头
        platform = str(r.get("platform") or "").strip()
        aid = str(r.get("account_id") or "").strip()
        name = str(r.get("account_name") or "").strip()
        if not platform and not aid and not name:
            continue  # 全空行忽略
        if platform not in PLATFORMS:
            errors.append((i, f"平台名错: {platform!r}"))
            continue
        if not aid:
            errors.append((i, "account_id 为空"))
            continue
        valid.append({"platform": platform, "account_id": aid, "account_name": name or aid})
    return valid, errors
```

- [ ] **Step 6: 跑测试确认通过**

Run: `uv run pytest tests/test_accounts.py -q`
Expected: PASS（9 passed）

- [ ] **Step 7: Commit**

```bash
git add src/collector/accounts.py tests/test_accounts.py tests/fixtures/import_sample.csv tests/fixtures/import_sample.xlsx
git commit -m "feat(accounts): parse_import_file 支持 csv/xlsx + 样例 fixture"
```

---

## Task 5: cli.py —— `account` 子命令组（list/add/remove/import/template）

**Files:**
- Modify: `cli.py`

> 这些是交互式/打印为主的胶水代码，逻辑都在 accounts.py（已单测）。本任务不写新单测，靠手动验证。

- [ ] **Step 1: 在 cli.py 顶部加 import**

在现有 import 区加：
```python
import csv as _csv  # noqa: F401  (占位，确保 csv 模块在 template 用到时可用；若未用可删)
from openpyxl import Workbook

from collector.accounts import (
    PLATFORMS as ACCOUNT_PLATFORMS,
    add_account,
    load_accounts_raw,
    parse_import_file,
    remove_account,
    validate_account_id,
    write_accounts,
)
```
（若 `_csv` 未被使用，删掉该行；保留 `Workbook` 给 template 用。）

- [ ] **Step 2: 定义 account 子命令组并挂到 app**

在 `app = typer.Typer(...)` 之后加：
```python
account_app = typer.Typer(help="账号清单管理（免编 YAML）")
app.add_typer(account_app, name="account")


def _pick_platform() -> str:
    typer.echo("选平台：")
    for i, p in enumerate(ACCOUNT_PLATFORMS, start=1):
        typer.echo(f"  [{i}] {p}")
    idx = typer.prompt("输入序号", type=int)
    if not 1 <= idx <= len(ACCOUNT_PLATFORMS):
        raise typer.BadParameter("序号超出范围")
    return ACCOUNT_PLATFORMS[idx - 1]
```

- [ ] **Step 3: `account list`**

```python
@account_app.command("list")
def account_list():
    """列出四平台所有账号。"""
    from rich.console import Console
    from rich.table import Table

    table = Table(title="账号清单")
    table.add_column("平台")
    table.add_column("account_id")
    table.add_column("名称")
    for name in ACCOUNT_PLATFORMS:
        for a in load_accounts_raw(CONFIG_ROOT, name):
            table.add_row(name, a["account_id"], a["account_name"])
    Console().print(table)
```

- [ ] **Step 4: `account add`**

```python
@account_app.command("add")
def account_add(
    platform: str = typer.Argument(None),
    account_id: str = typer.Argument(None),
    name: str = typer.Argument(None),
):
    """加一个账号。省略参数则交互式提问。"""
    if platform is None:
        platform = _pick_platform()
    elif platform not in ACCOUNT_PLATFORMS:
        raise typer.BadParameter(f"平台须是 {ACCOUNT_PLATFORMS} 之一")
    if account_id is None:
        account_id = typer.prompt("account_id")
    if name is None:
        name = typer.prompt("账号名称", default=account_id)

    warn = validate_account_id(platform, account_id)
    if warn:
        typer.secho(f"⚠️ {warn}", fg=typer.colors.YELLOW)
    if add_account(CONFIG_ROOT, platform, account_id, name):
        typer.secho(f"✅ 已加入 configs/{platform}.yaml", fg=typer.colors.GREEN)
    else:
        typer.secho(f"已存在，未重复添加：{account_id}", fg=typer.colors.YELLOW)
```

- [ ] **Step 5: `account remove`**

```python
@account_app.command("remove")
def account_remove(
    platform: str = typer.Argument(None),
    account_id: str = typer.Argument(None),
):
    """删一个账号。省略参数则交互式选择。"""
    if platform is None:
        platform = _pick_platform()
    elif platform not in ACCOUNT_PLATFORMS:
        raise typer.BadParameter(f"平台须是 {ACCOUNT_PLATFORMS} 之一")
    if account_id is None:
        rows = load_accounts_raw(CONFIG_ROOT, platform)
        if not rows:
            typer.echo("该平台暂无账号")
            raise typer.Exit()
        for i, a in enumerate(rows, start=1):
            typer.echo(f"  [{i}] {a['account_id']}  {a['account_name']}")
        idx = typer.prompt("删哪个（序号）", type=int)
        if not 1 <= idx <= len(rows):
            raise typer.BadParameter("序号超出范围")
        account_id = rows[idx - 1]["account_id"]

    if remove_account(CONFIG_ROOT, platform, account_id):
        typer.secho(f"✅ 已从 configs/{platform}.yaml 删除 {account_id}", fg=typer.colors.GREEN)
    else:
        typer.secho(f"未找到：{account_id}", fg=typer.colors.YELLOW)
```

- [ ] **Step 6: `account import`**

```python
@account_app.command("import")
def account_import(file: str):
    """从 .xlsx / .csv 批量导入账号（三列：platform / account_id / account_name）。"""
    valid, errors = parse_import_file(Path(file))
    added = skipped = 0
    # 按平台分组写入，去重
    by_platform: dict[str, list[dict]] = {}
    for v in valid:
        by_platform.setdefault(v["platform"], []).append(v)
    for platform, rows in by_platform.items():
        for v in rows:
            if add_account(CONFIG_ROOT, platform, v["account_id"], v["account_name"]):
                added += 1
            else:
                skipped += 1
    msg = f"新增 {added} 个 · 跳过 {skipped} 个(已存在) · 无效 {len(errors)} 个"
    typer.secho(msg, fg=typer.colors.GREEN)
    for row_no, reason in errors:
        typer.secho(f"  无效 row{row_no}: {reason}", fg=typer.colors.YELLOW)
```

- [ ] **Step 7: `account template`**

```python
@account_app.command("template")
def account_template(out: str = typer.Argument("account_template.xlsx")):
    """生成带表头的空导入模板 xlsx。"""
    wb = Workbook()
    ws = wb.active
    ws.append(["platform", "account_id", "account_name"])
    wb.save(out)
    typer.secho(f"✅ 模板已写 {out}（platform 填 bilibili/weibo/douyin/kuaishou）", fg=typer.colors.GREEN)
```

- [ ] **Step 8: 手动验证全套**

Run:
```bash
uv run python cli.py account template /tmp/tpl.xlsx
uv run python cli.py account add bilibili 999888 测试号
uv run python cli.py account list
uv run python cli.py account remove bilibili 999888
uv run python cli.py account import tests/fixtures/import_sample.csv
```
Expected: template 生成成功；add 后 list 能看到「测试号」；remove 后消失；import 打印 `新增 2 个 · 跳过 0 个 · 无效 1 个` 并列出 row4 无效。
验证后把 import 进 configs 的测试账号清理掉（手动删或 `git checkout configs/`）。

- [ ] **Step 9: 跑全套测试确保没退化**

Run: `uv run pytest -q`
Expected: PASS（全绿）

- [ ] **Step 10: Commit**

```bash
git checkout configs/   # 丢弃手动验证写进去的测试账号
git add cli.py
git commit -m "feat(cli): account 子命令组 list/add/remove/import/template"
```

---

## Task 6: status.py —— cookie_advice 纯函数

**Files:**
- Modify: `src/collector/status.py`
- Test: `tests/test_status.py`

- [ ] **Step 1: 写失败测试（追加进 tests/test_status.py）**

```python
from collector.cookies import save_cookies
from collector.status import cookie_advice


def test_cookie_advice_missing(tmp_path: Path):
    age, advice = cookie_advice("kuaishou", tmp_path / "nope.json", None)
    assert age == "—"
    assert "未登录" in advice


def test_cookie_advice_expired(tmp_path: Path):
    p = tmp_path / "weibo.json"
    save_cookies(p, platform="weibo", cookies=[{"name": "a", "value": "1"}])
    age, advice = cookie_advice("weibo", p, "expired")
    assert "已过期" in advice


def test_cookie_advice_fresh_ok(tmp_path: Path):
    p = tmp_path / "bilibili.json"
    save_cookies(p, platform="bilibili", cookies=[{"name": "a", "value": "1"}])
    age, advice = cookie_advice("bilibili", p, "ok")
    assert advice.startswith("✅")
    assert age == "0天"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_status.py -k cookie_advice -q`
Expected: FAIL（`ImportError: cannot import name 'cookie_advice'`）

- [ ] **Step 3: 写实现（加进 src/collector/status.py）**

文件顶部 import 区加：
```python
from collector.cookies import CookieFileMissing, cookie_age
```

在文件中加常量与函数：
```python
# cookie 年龄预警阈值（天）。快手是 7 天会话，提前 1 天预警。
COOKIE_AGE_WARN_DAYS = {"kuaishou": 6, "bilibili": 25, "weibo": 25, "douyin": 25}


def cookie_advice(
    platform: str, cookie_path: Path, last_run_health: str | None
) -> tuple[str, str]:
    """返回 (年龄文本, 建议)。优先级：缺文件 > 上次运行已过期 > 年龄超阈值 > 正常。"""
    if not cookie_path.exists():
        return "—", "❌ 未登录，需扫码"
    try:
        days = cookie_age(cookie_path).days
    except CookieFileMissing:
        return "—", "❌ 未登录，需扫码"
    age_text = f"{days}天"
    if last_run_health == "expired":
        return age_text, "❌ 已过期，需重扫"
    if days > COOKIE_AGE_WARN_DAYS.get(platform, 25):
        return age_text, "⚠️ 快过期，建议重扫"
    return age_text, "✅ 正常"
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_status.py -q`
Expected: PASS（原有 + 3 新，全绿）

- [ ] **Step 5: Commit**

```bash
git add src/collector/status.py tests/test_status.py
git commit -m "feat(status): cookie_advice 纯函数（年龄+建议）"
```

---

## Task 7: status.py + cli.py —— 新状态面板

**Files:**
- Modify: `src/collector/status.py`、`cli.py`

- [ ] **Step 1: 在 status.py 加 render_status_panel，删旧 render_status_table**

删掉现有的 `render_status_table` 整个函数，替换为：
```python
def render_status_panel(
    platforms: list[str],
    cookie_root: Path,
    data_root: Path,
    run_status: "RunStatus | None",
) -> None:
    """实时面板：cookie 年龄（独立于上次运行）+ 近7天 + 建议。"""
    console = Console()
    table = Table(title="新媒体采集状态")
    table.add_column("平台")
    table.add_column("Cookie 年龄")
    table.add_column("近7天")
    table.add_column("建议")
    for name in platforms:
        health = None
        if run_status is not None and name in run_status.platforms:
            health = run_status.platforms[name].cookie_health
        age_text, advice = cookie_advice(name, cookie_root / f"{name}.json", health)
        n7 = count_recent_posts(data_root, name)
        table.add_row(name, age_text, str(n7), advice)
    console.print(table)
    console.print("重扫命令：cli.py login <平台>")
```

- [ ] **Step 2: 改 cli.py 的 status 命令用新面板**

把 cli.py 顶部 import 里的 `render_status_table` 改成 `render_status_panel`：
```python
from collector.status import (
    PlatformStatus,
    RunStatus,
    count_recent_posts,
    read_status,
    render_status_panel,
    write_status,
)
```
替换 status 命令体：
```python
@app.command()
def status():
    """显示采集健康面板：cookie 年龄 + 近7天 + 建议。"""
    rs = read_status(STATUS_PATH) if STATUS_PATH.exists() else None
    render_status_panel(list(PLATFORMS.keys()), COOKIE_ROOT, DATA_ROOT, rs)
```

- [ ] **Step 3: 跑全套测试确保没退化**

Run: `uv run pytest -q`
Expected: PASS（全绿）

- [ ] **Step 4: 手动验证面板**

Run: `uv run python cli.py status`
Expected: rich 表格四列（平台 / Cookie 年龄 / 近7天 / 建议）；已登录平台显示天数与 ✅，未登录平台显示 `—` 与 `❌ 未登录`；底部一行重扫命令提示。

- [ ] **Step 5: Commit**

```bash
git add src/collector/status.py cli.py
git commit -m "feat(status): cli status 改为实时面板（cookie 年龄+建议）"
```

---

## Self-Review（写计划后自查结论）

- **Spec 覆盖**：特性 A（status 年龄列 + 建议 + 阈值）→ Task 6/7；特性 B（list/add/remove/import/template + 校验 + 去重 + 安全写 YAML）→ Task 1–5；测试策略 → 各任务内 tmp_path 单测；验收标准 5 条均有对应任务覆盖。无遗漏。
- **占位符**：无 TBD/TODO；每个 code 步骤都给了完整代码。
- **类型一致**：`load_accounts_raw / add_account / remove_account / validate_account_id / parse_import_file / write_accounts` 在 accounts.py 定义、在 cli.py 引用，签名一致；`cookie_advice` 返回 `(str, str)` 在 Task 6 定义、Task 7 解包一致；`render_status_panel` 形参与 cli 调用一致。
